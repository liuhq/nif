# A TOML linter such as https://taplo.tamasfe.dev/ can use this schema to validate your config.
# If you encounter any issues, please make an issue at https://github.com/yazi-rs/schemas.
"$schema" = "https://yazi-rs.github.io/schemas/yazi.json"

[mgr]
ratio = [1, 5, 4]
sort_by = "natural"
sort_sensitive = false
sort_reverse = false
sort_dir_first = false
sort_translit = true
linemode = "size"
show_hidden = false
show_symlink = true
scrolloff = 5
mouse_events = ["click", "scroll"]
title_format = "Yazi: {cwd}"

[preview]
wrap = "yes"
tab_size = 2
max_width = 1000
max_height = 1000
image_delay = 5
image_filter = "nearest"
image_quality = 75
ueberzug_scale = 1
ueberzug_offset = [0, 0, 0, 0]

[opener]
edit = [
    { run = '${EDITOR:-vi} "$@"', desc = "env EDITOR", block = true, for = "unix" },
]
open = [{ run = 'xdg-open "$1"', desc = "Open", for = "linux" }]
reveal = [
    { run = 'xdg-open "$(dirname "$1")"', desc = "Reveal", for = "linux" },
    { run = '''exiftool "$1"; echo "Press enter to exit"; read _''', block = true, desc = "Show EXIF", for = "unix" },
]
extract = [
    { run = 'ya pub extract --list "$@"', desc = "Extract here", for = "unix" },
]
play = [
    { run = 'mpv --force-window "$@"', orphan = true, for = "unix" },
    { run = '''mediainfo "$1"; echo "Press enter to exit"; read _''', block = true, desc = "Show media info", for = "unix" },
]
imv = [
    { run = 'imv "$@"', orphan = true, desc = "Open images via imv", for = "unix" },
]
imvd = [
    { run = 'imv-dir "$0"', orphan = true, desc = "Open images in directory via imv", for = "unix" },
]

[open]
rules = [
    # Folder
    { url = "*/", use = [
        "edit",
        "open",
        "reveal",
    ] },
    # Text
    { mime = "text/*", use = [
        "edit",
        "reveal",
    ] },
    # Image
    { mime = "image/*", use = [
        "imv",
        "imvd",
        "reveal",
    ] },
    # Media
    { mime = "{audio,video}/*", use = [
        "play",
        "reveal",
    ] },
    # Archive
    { mime = "application/{,g}zip", use = [
        "extract",
        "reveal",
    ] },
    { mime = "application/{tar,bzip*,7z*,xz,rar,zstd}", use = [
        "extract",
        "reveal",
    ] },
    # JSON
    { mime = "application/{json,ndjson}", use = [
        "edit",
        "reveal",
    ] },
    # pdf
    { mime = "application/pdf", use = [
        "open",
        "reveal",
    ] },
    { mime = "*/javascript", use = [
        "edit",
        "reveal",
    ] },
    # Empty file
    { mime = "inode/empty", use = [
        "edit",
        "reveal",
    ] },
    # Fallback
    { url = "*", use = [
        "open",
        "edit",
        "reveal",
    ] },
]

[tasks]
micro_workers = 10
macro_workers = 25
bizarre_retry = 5
image_alloc = 536870912  # 512MB
image_bound = [0, 0]
suppress_preload = false

[plugin]
prepend_fetchers = [
    { id = "git", url = "*", run = "git" },
    { id = "git", url = "*/", run = "git" },
    { id = "mime", url = "local://*", run = "mime-ext.local", prio = "high" },
    { id = "mime", url = "remote://*", run = "mime-ext.remote", prio = "high" },
]
prepend_preloaders = [
    # Image
    { mime = "image/{avif,hei?,jxl,svg+xml}", run = "magick" },
    # { mime = "image/*", run = "image" },
    # Video
    # { mime = "video/*", run = "video" },
    # PDF
    # { mime = "application/pdf", run = "pdf" },
    # Font
    { mime = "font/*", run = "font" },
    { mime = "application/ms-opentype", run = "font" },
]
prepend_previewers = [
    # { url = "*/", run = "folder", sync = true },
    # Code
    # { mime = "text/*", run = "code" },
    # { mime = "*/{xml,javascript,wine-extension-ini}", run = "code" },
    # JSON
    # { mime = "application/{json,ndjson}", run = "json" },
    # Image
    { mime = "image/{avif,hei?,jxl,svg+xml}", run = "magick" },
    # { mime = "image/*", run = "image" },
    # Video
    # { mime = "video/*", run = "video" },
    # PDF
    # { mime = "application/pdf", run = "pdf" },
    # Archive
    # { mime = "application/{,g}zip", run = "archive" },
    # { mime = "application/{tar,bzip*,7z*,xz,rar,zstd,iso9660-image}", run = "archive" },
    # Font
    { mime = "font/*", run = "font" },
    { mime = "application/ms-opentype", run = "font" },
    # Empty file
    # { mime = "inode/empty", run = "empty" },
]
append_previewers = [
    # Fallback
    { url = "*", run = "file" },
]

[input]
cursor_blink = false

# cd
cd_title = "Change directory:"
cd_origin = "bottom-left"
cd_offset = [0, -1, 50, 3]

# create
create_title = ["Create:", "Create (dir):"]
create_origin = "bottom-left"
create_offset = [0, -1, 50, 3]

# rename
rename_title = "Rename:"
rename_origin = "hovered"
rename_offset = [0, 1, 50, 3]

# filter
filter_title = "Filter:"
filter_origin = "bottom-left"
filter_offset = [0, -1, 50, 3]

# find
find_title = ["Find next:", "Find previous:"]
find_origin = "bottom-left"
find_offset = [0, -1, 50, 3]

# search
search_title = "Search via {n}:"
search_origin = "bottom-left"
search_offset = [0, -1, 50, 3]

# shell
shell_title = ["Shell:", "Shell (block):"]
shell_origin = "bottom-left"
shell_offset = [0, -1, 50, 3]

[confirm]
# trash
trash_title = "Trash {n} selected file{s}?"
trash_origin = "center"
trash_offset = [0, 0, 70, 20]

# delete
delete_title = "Permanently delete {n} selected file{s}?"
delete_origin = "center"
delete_offset = [0, 0, 70, 20]

# overwrite
overwrite_title = "Overwrite file?"
overwrite_origin = "center"
overwrite_offset = [0, 0, 50, 15]

# quit
quit_title = "Quit?"
quit_origin = "center"
quit_offset = [0, 0, 50, 15]

[pick]
open_title = "Open with:"
open_origin = "hovered"
open_offset = [0, 1, 50, 7]

[which]
sort_by = "none"
sort_sensitive = false
sort_reverse = false
sort_translit = true
